export const seasons = (seasons) => {
  return {
    type: 'SEASONS',
    seasons: seasons 
  }
}

export const seasonID = (seasonID) => {
  return {
    type: 'SEASONSID',
    seasonID: seasonID 
  }
}